#pragma once

#ifndef GAL_LANG_SOURCE_CHUNK_HPP
#define GAL_LANG_SOURCE_CHUNK_HPP

#include <cstring>

namespace gal::vm
{
	inline void get_chunk_id(char* out, const char* source, std::size_t size)
	{
		(void)out;
		(void)source;
		(void)size;

		// todo: implement it
	}
}

#endif // GAL_LANG_SOURCE_CHUNK_HPP
